{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>

    <ul>
        {% for user in users %}
            <li>{{ user.name }}</li>
            <a href="{{ path('app_detail', {'id': user.id}) }}">Liste des utilisateurs</a>

        {% endfor %}
    </ul>
{% endblock %}